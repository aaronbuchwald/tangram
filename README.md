# feedback-assets

Asset store for the Tangram **feedback** app: screenshots are uploaded via the
GitHub Contents API targeting THIS branch (never `main`, never merged), and the
raw URL is embedded in the issue body.

Stopgap mechanism — see issue #37 for why it exists and the plan to replace it.
Do NOT merge this branch into `main`.