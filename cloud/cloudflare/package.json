{
  "name": "tangram-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Tangram app host on Cloudflare Workers + Durable Objects: per-app UI/api/sync/mcp over jco-transpiled app components (ADR-0002); the sync surface speaks docs/SYNC_PROTOCOL.md",
  "scripts": {
    "build:components": "bash build-components.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@automerge/automerge": "^3.2.6"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "1.22.0",
    "@cloudflare/workers-types": "^4.20260610.0",
    "typescript": "^5.6.0",
    "wrangler": "~4.86.0"
  }
}
